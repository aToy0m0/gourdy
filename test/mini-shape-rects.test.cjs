const test=require('node:test'),assert=require('node:assert/strict'),{miniShapeRects}=require('../src/mini-shape-rects.cjs');
test('CSS rectangles follow page zoom into native DIP',()=>{assert.deepEqual(miniShapeRects([{x:36,y:80,width:106,height:106}],{width:224,height:278},{width:280,height:348},1.25),[{x:45,y:100,width:133,height:133}]);});
test('stale viewport is ignored and invalid rectangles are rejected',()=>{assert.equal(miniShapeRects([{x:0,y:0,width:1,height:1}],{width:140,height:174},{width:280,height:348},1),null);assert.throws(()=>miniShapeRects([{x:139,y:0,width:2,height:1}],{width:140,height:174},{width:140,height:174},1),/形状/);});
test('fractional zoom keeps every native rectangle inside the window',()=>{for(const zoom of [.8,1,1.1,1.25,1.5,2]){const size={width:420,height:522},viewport={width:Math.round(size.width/zoom),height:Math.round(size.height/zoom)};const rects=miniShapeRects([{x:0,y:0,width:viewport.width,height:viewport.height}],viewport,size,zoom);assert.ok(rects);assert.ok(rects.every(r=>r.x>=0&&r.y>=0&&r.x+r.width<=size.width&&r.y+r.height<=size.height));}});
